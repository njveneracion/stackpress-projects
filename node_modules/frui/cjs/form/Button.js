"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = Button;
const jsx_runtime_1 = require("react/jsx-runtime");
function Button(props) {
    const { href, target, title, block, full, color, xs, sm, md, lg, xl, xl2, xl3, xl4, xl5, curved, rounded, pill, info, warning, success, error, muted, outline, transparent, solid, style, className, children } = props, attributes = __rest(props, ["href", "target", "title", "block", "full", "color", "xs", "sm", "md", "lg", "xl", "xl2", "xl3", "xl4", "xl5", "curved", "rounded", "pill", "info", "warning", "success", "error", "muted", "outline", "transparent", "solid", "style", "className", "children"]);
    const defaults = {
        classes: ['frui-btn'],
        styles: {}
    };
    if (block) {
        defaults.classes.push('frui-block');
    }
    if (full) {
        defaults.classes.push('frui-full');
    }
    const size = xs ? 'xs'
        : sm ? 'sm'
            : md ? 'md'
                : lg ? 'lg'
                    : xl ? 'xl'
                        : xl2 ? '2xl'
                            : xl3 ? '3xl'
                                : xl4 ? '4xl'
                                    : xl5 ? '5xl'
                                        : 'md';
    defaults.classes.push(`frui-btn-${size}`);
    const layout = outline
        ? 'outline'
        : transparent
            ? 'transparent'
            : solid
                ? 'solid'
                : 'solid';
    if (curved) {
        defaults.classes.push('frui-curved');
    }
    else if (rounded) {
        defaults.classes.push('frui-rounded');
    }
    else if (pill) {
        defaults.classes.push('frui-pill');
    }
    if (layout === 'outline' || layout === 'transparent') {
        defaults.classes.push('frui-solid', 'frui-thin');
        if (layout === 'outline') {
            defaults.classes.push('frui-bg-white');
        }
        if (color) {
            defaults.styles.borderColor = color;
            defaults.styles.color = color;
        }
        else if (info) {
            defaults.classes.push('frui-bd-info', 'frui-tx-info');
        }
        else if (warning) {
            defaults.classes.push('frui-bd-warning', 'frui-tx-warning');
        }
        else if (success) {
            defaults.classes.push('frui-bd-success', 'frui-tx-success');
        }
        else if (error) {
            defaults.classes.push('frui-bd-error', 'frui-tx-error');
        }
        else if (muted) {
            defaults.classes.push('frui-bd-muted', 'frui-tx-muted');
        }
    }
    else {
        defaults.classes.push('frui-tx-white');
        if (color) {
            defaults.styles.backgroundColor = color;
        }
        else if (info) {
            defaults.classes.push('frui-bg-info');
        }
        else if (warning) {
            defaults.classes.push('frui-bg-warning');
        }
        else if (success) {
            defaults.classes.push('frui-bg-success');
        }
        else if (error) {
            defaults.classes.push('frui-bg-error');
        }
        else if (muted) {
            defaults.classes.push('frui-bg-muted');
        }
    }
    const map = {
        classes: [...defaults.classes, className].join(' '),
        styles: Object.assign(Object.assign({}, defaults.styles), style)
    };
    if (href) {
        return ((0, jsx_runtime_1.jsx)("a", { href: href, target: target, title: title, className: map.classes, style: map.styles, children: children }));
    }
    return ((0, jsx_runtime_1.jsx)("button", Object.assign({ title: title, className: map.classes, style: map.styles }, attributes, { children: children })));
}
;
