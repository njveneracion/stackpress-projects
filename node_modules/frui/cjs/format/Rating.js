"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = Rating;
const jsx_runtime_1 = require("react/jsx-runtime");
function Rating(props) {
    const { value, max, remainder, round, } = props;
    const rating = round === 'round' ? Math.round(Number(value))
        : round === 'ceil' ? Math.ceil(Number(value))
            : round === 'floor' ? Math.floor(Number(value))
                : Math.round(Number(value));
    const stars = [];
    for (let i = 0; i < (max || rating); i++) {
        if (i < rating) {
            stars.push((0, jsx_runtime_1.jsx)("span", { children: "\u2605" }, i));
        }
        else if (remainder) {
            stars.push((0, jsx_runtime_1.jsx)("span", { children: "\u2606" }, i));
        }
    }
    return ((0, jsx_runtime_1.jsx)(jsx_runtime_1.Fragment, { children: stars }));
}
;
