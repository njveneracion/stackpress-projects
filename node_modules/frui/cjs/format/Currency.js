"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = Currency;
const jsx_runtime_1 = require("react/jsx-runtime");
const currencies_js_1 = __importDefault(require("../data/currencies.js"));
const countries_js_1 = __importDefault(require("../data/countries.js"));
function Currency(props) {
    const { value, flag = true, text = true, className, style = {} } = props;
    const currency = currencies_js_1.default
        .filter(currency => currency.type === 'fiat')
        .map(currency => {
        var _a;
        return (Object.assign(Object.assign({}, currency), { flag: (_a = countries_js_1.default.find(country => country.cur === currency.code)) === null || _a === void 0 ? void 0 : _a.flag }));
    })
        .filter(currency => !!currency.flag)
        .find(currency => (currency.code === value));
    if (!currency) {
        const classNames = ['frui-format-country-text'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsx)("span", { className: classNames.join(' '), style: style, children: value }));
    }
    if (flag && text) {
        const classNames = ['frui-format-country'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsxs)("span", { className: classNames.join(' '), style: style, children: [(0, jsx_runtime_1.jsx)("span", { className: "frui-format-country-flag", children: currency.flag }), (0, jsx_runtime_1.jsx)("span", { className: "frui-format-country-text", children: currency.name })] }));
    }
    else if (flag) {
        const classNames = ['frui-format-country-flag'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsx)("span", { className: "frui-format-country-flag", children: currency.flag }));
    }
    const classNames = ['frui-format-country-text'];
    if (className) {
        classNames.push(className);
    }
    return ((0, jsx_runtime_1.jsx)("span", { className: classNames.join(' '), style: style, children: currency.name }));
}
;
