"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = Country;
const jsx_runtime_1 = require("react/jsx-runtime");
const countries_js_1 = __importDefault(require("../data/countries.js"));
function Country(props) {
    const { value, flag = true, text = true, className, style = {} } = props;
    const country = countries_js_1.default.find(country => country.iso3 === value);
    if (!country) {
        const classNames = ['frui-format-country-text'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsx)("span", { className: classNames.join(' '), style: style, children: value }));
    }
    if (flag && text) {
        const classNames = ['frui-format-country'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsxs)("span", { className: classNames.join(' '), style: style, children: [(0, jsx_runtime_1.jsx)("span", { className: "frui-format-country-flag", children: country.flag }), (0, jsx_runtime_1.jsx)("span", { className: "frui-format-country-text", children: country.name })] }));
    }
    else if (flag) {
        const classNames = ['frui-format-country-flag'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsx)("span", { className: "frui-format-country-flag", children: country.flag }));
    }
    const classNames = ['frui-format-country-text'];
    if (className) {
        classNames.push(className);
    }
    return ((0, jsx_runtime_1.jsx)("span", { className: classNames.join(' '), style: style, children: country.name }));
}
;
