"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = Color;
const jsx_runtime_1 = require("react/jsx-runtime");
function Color(props) {
    const { value, box = true, text = true, sm, md, lg, className, style = {} } = props;
    const size = sm ? '8px' : md ? '12px' : lg ? '16px' : '12px';
    const boxStyles = {
        backgroundColor: value,
        height: size,
        width: size
    };
    if (box && text) {
        const classNames = ['frui-format-color'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsxs)("span", { className: classNames.join(' '), style: style, children: [(0, jsx_runtime_1.jsx)("span", { className: "frui-format-color-box", style: boxStyles }), (0, jsx_runtime_1.jsx)("span", { className: "frui-format-color-text", children: value })] }));
    }
    else if (box) {
        const classNames = ['frui-format-color-box'];
        if (className) {
            classNames.push(className);
        }
        return ((0, jsx_runtime_1.jsx)("span", { className: classNames.join(' '), style: Object.assign(Object.assign({}, style), boxStyles) }));
    }
    const classNames = ['frui-format-color-text'];
    if (className) {
        classNames.push(className);
    }
    return ((0, jsx_runtime_1.jsx)("span", { className: classNames.join(' '), style: style, children: value }));
}
;
