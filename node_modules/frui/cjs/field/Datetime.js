"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDatetime = useDatetime;
exports.default = Datetime;
const jsx_runtime_1 = require("react/jsx-runtime");
const Date_js_1 = require("./Date.js");
const Input_js_1 = __importDefault(require("./Input.js"));
function useDatetime({ defaultValue, onUpdate }) {
    const update = (0, Date_js_1.useDate)({ onUpdate });
    let value = undefined;
    if (defaultValue) {
        try {
            value = new Date(defaultValue).toISOString().replace('Z', '');
        }
        catch (e) { }
    }
    return { value, update };
}
;
function Datetime(props) {
    const { defaultValue, className, onUpdate } = props, attributes = __rest(props, ["defaultValue", "className", "onUpdate"]);
    const { value, update } = useDatetime({ defaultValue, onUpdate });
    const classNames = ['frui-field-datetime'];
    if (className) {
        classNames.push(className);
    }
    return ((0, jsx_runtime_1.jsx)(Input_js_1.default, Object.assign({}, attributes, { type: "datetime-local", defaultValue: value, className: classNames.join(' '), onUpdate: update })));
}
;
