"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTime = useTime;
exports.default = Time;
const jsx_runtime_1 = require("react/jsx-runtime");
const Input_js_1 = __importDefault(require("./Input.js"));
function useTime({ defaultValue }) {
    let value = undefined;
    if (defaultValue) {
        try {
            const [date, min] = new Date(defaultValue).toISOString().split(':');
            value = [date.split('T')[1], min].join(':');
        }
        catch (e) { }
    }
    return value;
}
;
function Time(props) {
    const { defaultValue, className } = props, attributes = __rest(props, ["defaultValue", "className"]);
    const value = useTime({ defaultValue });
    const classNames = ['frui-field-time'];
    if (className) {
        classNames.push(className);
    }
    return ((0, jsx_runtime_1.jsx)(Input_js_1.default, Object.assign({ type: "time", className: classNames.join(' '), defaultValue: value }, attributes)));
}
