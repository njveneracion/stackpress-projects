"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDate = useDate;
exports.default = DateField;
const jsx_runtime_1 = require("react/jsx-runtime");
const Input_js_1 = __importDefault(require("./Input.js"));
function useDate({ onUpdate }) {
    return (value) => {
        if (onUpdate && value) {
            try {
                const utc = new Date(value).toUTCString();
                onUpdate(new Date(utc).toISOString());
            }
            catch (e) { }
        }
    };
}
;
function DateField(props) {
    const { defaultValue, className, onUpdate } = props, attributes = __rest(props, ["defaultValue", "className", "onUpdate"]);
    const update = useDate({ onUpdate });
    const classNames = ['frui-field-date'];
    if (className) {
        classNames.push(className);
    }
    return ((0, jsx_runtime_1.jsx)(Input_js_1.default, Object.assign({}, attributes, { type: "date", defaultValue: defaultValue
            ? new Date(defaultValue).toISOString().split('T')[0]
            : undefined, className: classNames.join(' '), onUpdate: update })));
}
;
