import { jsx as _jsx } from "react/jsx-runtime";
export function useInput({ onChange, onUpdate }) {
    return {
        handlers: {
            change: (e) => {
                onChange && onChange(e);
                onUpdate && onUpdate(e.target.value);
            }
        }
    };
}
export default function Input(props) {
    const { error, className, onChange, onUpdate, passRef, ...attributes } = props;
    const { handlers } = useInput({ onChange, onUpdate });
    const classNames = ['frui-field-input'];
    if (error) {
        classNames.push('frui-tx-error', 'frui-bd-error');
    }
    if (className) {
        classNames.push(className);
    }
    return (_jsx("input", { ...attributes, className: classNames.join(' '), ref: passRef, onChange: handlers.change }));
}
;
