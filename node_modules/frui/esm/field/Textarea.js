import { jsx as _jsx } from "react/jsx-runtime";
export function useTextarea({ onChange, onUpdate }) {
    return {
        handlers: {
            change: (e) => {
                onChange && onChange(e);
                onUpdate && onUpdate(e.target.value);
            }
        }
    };
}
export default function Textarea(props) {
    const { error, className, onChange, onUpdate, passRef, ...attributes } = props;
    const { handlers } = useTextarea({ onChange, onUpdate });
    const classNames = ['frui-field-textarea'];
    if (error) {
        classNames.push('frui-tx-error', 'frui-bd-error');
    }
    if (className) {
        classNames.push(className);
    }
    return (_jsx("textarea", { ...attributes, className: classNames.join(' '), ref: passRef, onChange: handlers.change }));
}
;
