import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState } from 'react';
import Input from './Input.js';
export function usePassword() {
    const [showing, show] = useState(false);
    const toggle = () => show(!showing);
    return { toggle, showing };
}
export default function Password(props) {
    const { error, className, ...attributes } = props;
    const { showing, toggle } = usePassword();
    const classNames = ['frui-field-password'];
    if (className) {
        classNames.push(className);
    }
    const toggleClass = ['frui-field-password-toggle'];
    if (error) {
        toggleClass.push('frui-tx-error', 'frui-bd-error');
    }
    return (_jsxs("div", { className: classNames.join(' '), children: [_jsx(Input, { ...attributes, error: error, type: showing ? 'text' : 'password', className: "frui-field-password-control" }), _jsx("span", { className: toggleClass.join(' '), onClick: toggle, children: showing ? '✷' : 'A' })] }));
}
;
