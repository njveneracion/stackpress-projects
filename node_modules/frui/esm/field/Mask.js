import { jsx as _jsx } from "react/jsx-runtime";
import Input from './Input.js';
import 'inputmask';
export default function InputMask(props) {
    const { mask, regex, alias, repeat, greedy, numericInput, rightAlign, definitions, onReady, ...attributes } = props;
    const ref = (ref) => {
        if (!ref)
            return;
        const im = new Inputmask({
            mask,
            regex,
            alias,
            repeat,
            greedy,
            numericInput,
            rightAlign,
            definitions
        });
        im.mask(ref);
    };
    return _jsx(Input, { passRef: ref, ...attributes });
}
;
