import { jsx as _jsx } from "react/jsx-runtime";
export default function Separated(props) {
    const { value, className, style, separator = ' ' } = props;
    if (separator === 'line') {
        return (_jsx("div", { className: className, style: style, children: value.map((value, i) => _jsx("div", { children: value }, i)) }));
    }
    return (_jsx("span", { className: className, style: style, children: value.join(separator) }));
}
;
