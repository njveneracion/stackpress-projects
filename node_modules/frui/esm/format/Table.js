import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Table, Thead, Tcol, Trow } from '../element/Table.js';
export default function TableFormat(props) {
    const { style = {}, className, value, stripes } = props;
    if (!value || !value.length)
        return null;
    const styles = {
        head: {
            ...style,
            textAlign: 'left',
            backgroundColor: stripes ? stripes[0] : undefined
        },
        rows: [
            { ...style, backgroundColor: stripes ? stripes[1] : undefined },
            { ...style, backgroundColor: stripes ? stripes[2] : undefined }
        ]
    };
    return (_jsxs(Table, { children: [Object.keys(value[0]).map((key) => (_jsx(Thead, { className: className, style: styles.head, children: key }, key))), value.map((row, i) => (_jsx(Trow, { children: Object.values(row).map((value, j) => (_jsx(Tcol, { className: className, style: styles.rows[i % 2], children: value }, `${i}-${j}`))) }, i)))] }));
}
;
