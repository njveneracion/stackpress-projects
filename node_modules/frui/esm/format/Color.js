import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
export default function Color(props) {
    const { value, box = true, text = true, sm, md, lg, className, style = {} } = props;
    const size = sm ? '8px' : md ? '12px' : lg ? '16px' : '12px';
    const boxStyles = {
        backgroundColor: value,
        height: size,
        width: size
    };
    if (box && text) {
        const classNames = ['frui-format-color'];
        if (className) {
            classNames.push(className);
        }
        return (_jsxs("span", { className: classNames.join(' '), style: style, children: [_jsx("span", { className: "frui-format-color-box", style: boxStyles }), _jsx("span", { className: "frui-format-color-text", children: value })] }));
    }
    else if (box) {
        const classNames = ['frui-format-color-box'];
        if (className) {
            classNames.push(className);
        }
        return (_jsx("span", { className: classNames.join(' '), style: { ...style, ...boxStyles } }));
    }
    const classNames = ['frui-format-color-text'];
    if (className) {
        classNames.push(className);
    }
    return (_jsx("span", { className: classNames.join(' '), style: style, children: value }));
}
;
