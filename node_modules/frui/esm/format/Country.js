import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import countries from '../data/countries.js';
export default function Country(props) {
    const { value, flag = true, text = true, className, style = {} } = props;
    const country = countries.find(country => country.iso3 === value);
    if (!country) {
        const classNames = ['frui-format-country-text'];
        if (className) {
            classNames.push(className);
        }
        return (_jsx("span", { className: classNames.join(' '), style: style, children: value }));
    }
    if (flag && text) {
        const classNames = ['frui-format-country'];
        if (className) {
            classNames.push(className);
        }
        return (_jsxs("span", { className: classNames.join(' '), style: style, children: [_jsx("span", { className: "frui-format-country-flag", children: country.flag }), _jsx("span", { className: "frui-format-country-text", children: country.name })] }));
    }
    else if (flag) {
        const classNames = ['frui-format-country-flag'];
        if (className) {
            classNames.push(className);
        }
        return (_jsx("span", { className: "frui-format-country-flag", children: country.flag }));
    }
    const classNames = ['frui-format-country-text'];
    if (className) {
        classNames.push(className);
    }
    return (_jsx("span", { className: classNames.join(' '), style: style, children: country.name }));
}
;
