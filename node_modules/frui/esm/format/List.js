import { jsx as _jsx } from "react/jsx-runtime";
export default function List(props) {
    const { value, ordered, className, style = {} } = props;
    const classNames = ['frui-format-list'];
    if (className) {
        classNames.push(className);
    }
    if (ordered) {
        return (_jsx("ol", { className: classNames.join(' '), style: style, children: value.map((value, i) => _jsx("li", { children: value }, i)) }));
    }
    return (_jsx("ul", { className: classNames.join(' '), style: style, children: value.map((value, i) => _jsx("li", { children: value }, i)) }));
}
;
