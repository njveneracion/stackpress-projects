import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
import moment from 'moment';
moment.locale('short', {
    parentLocale: 'en',
    relativeTime: {
        future: "-%s",
        past: "%s",
        s: 'now',
        ss: 'now',
        m: "1m",
        mm: "%dm",
        h: "1h",
        hh: "%dh",
        d: "1d",
        dd: "%dd",
        w: "1w",
        ww: "%dw",
        M: "1M",
        MM: "%dM",
        y: "1y",
        yy: "%dy"
    }
});
export default function DateFormat(props) {
    const { value, locale = 'en', format = 'MMMM Do YYYY, h:mm:ss a' } = props;
    const date = new Date(value);
    if (format === 'ago') {
        return (_jsx(_Fragment, { children: moment(date, format).locale(locale).fromNow() }));
    }
    else if (format === 'a') {
        return (_jsx(_Fragment, { children: moment(date, format).locale('short').fromNow() }));
    }
    return (_jsx(_Fragment, { children: moment(date).locale(locale).format(format) }));
}
;
