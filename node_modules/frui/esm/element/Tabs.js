import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState } from 'react';
export default function Tabs(props) {
    const { tabs, panels, vertical = false, scrollable = false, centered = false, fullWidth = false, wrap = false, } = props;
    const [activeTab, setActiveTab] = useState(0);
    const tabListClass = `frui-tab-list 
  ${vertical ? 'frui-tab-list-vertical' : ''} 
  ${scrollable ? 'frui-tab-list-scrollable' : ''} 
  ${centered ? 'frui-tab-list-centered' : ''} 
  ${!vertical && fullWidth ? 'frui-tab-list-fullwidth' : ''}`;
    return (_jsx("div", { className: `frui-tabs ${wrap ? 'wrap' : ''} ${vertical ? 'frui-tabs-vertical' : ''}`, children: _jsxs("div", { className: "frui-tab-container", children: [_jsx("div", { className: tabListClass, children: tabs.map((tab, index) => (_jsx(Tab, { label: tab.label, icon: tab.icon, disabled: tab.disabled, isActive: activeTab === index, onClick: () => setActiveTab(index), style: tab.style, className: tab.className, vertical: vertical }, index))) }), _jsx("div", { className: "frui-tab-panels", children: panels.map((panel, index) => (_jsx(TabPanel, { isActive: activeTab === index, children: panel }, index))) })] }) }));
}
function Tab({ label, icon, disabled, isActive, onClick, style, className, vertical }) {
    const tabClass = `frui-tab ${isActive ? 'frui-tab-active' : ''} ${disabled ? 'frui-tab-disabled' : ''} 
                    ${vertical ? 'frui-tab-vertical' : ''} ${className || ''}`;
    const iconClass = `frui-tab-icon ${icon ? 'frui-tab-icon-show' : ''}`;
    return (_jsxs("div", { className: tabClass, style: style, onClick: disabled ? undefined : onClick, children: [icon && _jsx("span", { className: iconClass, children: icon }), _jsx("span", { className: "frui-tab-label", children: label })] }));
}
function TabPanel({ children, isActive, style, className }) {
    const panelClass = `frui-tab-panel ${isActive ? 'frui-tab-panel-active' : ''} ${className || ''}`;
    return (_jsx("div", { className: panelClass, style: style, children: children }));
}
