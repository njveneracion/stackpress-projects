import type { SessionRoute } from './types.js';
export declare const isRegExp: RegExp;
export declare function matchAnyEvent(permit: string, permissions: string[]): boolean;
export declare function matchAnyRoute(permit: SessionRoute, permissions: SessionRoute[]): boolean;
export declare function matchEvent(permit: string, permission: string): boolean;
export declare function matchRoute(permit: SessionRoute, permission: SessionRoute): boolean;
