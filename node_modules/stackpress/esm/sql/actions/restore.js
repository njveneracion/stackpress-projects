import detail from './detail.js';
import update from './update.js';
export default async function restore(model, engine, ids) {
    const active = model.active?.snake;
    if (active) {
        return await update(model, engine, ids, { [active]: true });
    }
    return await detail(model, engine, ids);
}
;
