import type { Directory } from 'ts-morph';
import type Registry from '../../schema/Registry.js';
export default function generate(directory: Directory, registry: Registry): void;
export declare function page(action: string, directory: Directory, registry: Registry): void;
