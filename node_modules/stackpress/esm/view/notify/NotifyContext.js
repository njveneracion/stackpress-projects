import { createContext } from 'react';
export const config = {
    position: 'bottom-center',
    autoClose: 5000,
    hideProgressBar: false,
    closeOnClick: true,
    pauseOnHover: true,
    draggable: true,
    theme: 'dark',
};
const NotifyContext = createContext({ config });
export default NotifyContext;
