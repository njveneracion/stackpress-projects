"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const Server_1 = require("@stackpress/ingest/Server");
exports.default = (0, Server_1.action)(function QueryScript(_req, res, ctx) {
    return __awaiter(this, void 0, void 0, function* () {
        const cli = ctx.plugin('cli');
        const database = ctx.plugin('database');
        if (!database) {
            (cli === null || cli === void 0 ? void 0 : cli.verbose) && cli.control.error('No database found');
            res.setError('No database found');
            return;
        }
        const query = process.argv.slice(2).pop();
        if (query) {
            console.log(yield database.query(query.replace(/\\/g, "'")));
        }
        res.setStatus(200);
    });
});
