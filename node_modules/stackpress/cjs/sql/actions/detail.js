"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = detail;
const Exception_js_1 = __importDefault(require("../../Exception.js"));
const helpers_js_1 = require("../helpers.js");
const search_js_1 = __importDefault(require("./search.js"));
function detail(model_1, engine_1, ids_1) {
    return __awaiter(this, arguments, void 0, function* (model, engine, ids, columns = ['*'], seed) {
        var _a;
        const filter = Object.fromEntries(model.ids.map(column => [
            column.name,
            ids[column.name]
        ]));
        if (model.active) {
            filter[model.active.name] = -1;
        }
        const response = yield (0, search_js_1.default)(model, engine, { columns, filter, take: 1 }, seed);
        if (response.code !== 200) {
            return response;
        }
        else if (!((_a = response.results) === null || _a === void 0 ? void 0 : _a[0])) {
            return (0, helpers_js_1.toErrorResponse)(Exception_js_1.default.for('Not Found').withCode(404));
        }
        response.results = response.results[0];
        return response;
    });
}
;
