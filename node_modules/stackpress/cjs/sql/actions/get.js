"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = get;
const search_js_1 = __importDefault(require("./search.js"));
function get(model_1, engine_1, key_1, value_1) {
    return __awaiter(this, arguments, void 0, function* (model, engine, key, value, columns = ['*'], seed) {
        const filter = { [key]: value };
        if (model.active) {
            filter[model.active.name] = -1;
        }
        const response = yield (0, search_js_1.default)(model, engine, { columns, filter, take: 1 }, seed);
        if (Array.isArray(response.results)) {
            response.results = response.results[0] || null;
        }
        return response;
    });
}
;
