import type { UnknownNest, NestedObject, StatusResponse } from '@stackpress/lib/types';
import type Engine from '@stackpress/inquire/Engine';
import type Model from '../../schema/spec/Model.js';
export default function create<M extends UnknownNest = UnknownNest>(model: Model, engine: Engine, input: NestedObject, seed?: string): Promise<StatusResponse<Partial<M>>>;
