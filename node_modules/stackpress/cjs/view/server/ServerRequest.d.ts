import type { Method, UnknownNest, CallableMap, CallableNest } from '@stackpress/lib/types';
import type { ServerRequestProps } from '../types.js';
export default class Request<I extends UnknownNest = UnknownNest> {
    readonly data: CallableNest;
    readonly headers: CallableMap<string, string | string[]>;
    readonly session: CallableMap<string, string | string[]>;
    readonly url: URL;
    readonly method: Method;
    constructor(config: ServerRequestProps<I>);
}
