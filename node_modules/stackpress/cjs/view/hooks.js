"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useStripe = useStripe;
function useStripe(color1, color2) {
    let active = color1;
    const toggle = (change) => {
        if (typeof change === 'number') {
            return change % 2 === 0 ? color1 : color2;
        }
        if (change) {
            active = active === color1 ? color2 : color1;
        }
        return active;
    };
    return toggle;
}
