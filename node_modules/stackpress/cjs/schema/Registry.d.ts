import type { EnumConfig, SchemaConfig } from '@stackpress/idea-parser/types';
import Fieldset from './spec/Fieldset.js';
import Model from './spec/Model.js';
export default class Registry {
    constructor(schema: SchemaConfig);
    readonly enum: Map<string, EnumConfig>;
    readonly fieldset: Map<string, Fieldset>;
    readonly model: Map<string, Model>;
}
