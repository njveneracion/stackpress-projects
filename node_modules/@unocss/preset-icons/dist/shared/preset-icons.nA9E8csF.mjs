import { b as createCDNFetchLoader } from './preset-icons.-TyRHmb4.mjs';

async function createCDNLoader(cdnBase) {
  const { $fetch } = await import('ofetch');
  return createCDNFetchLoader($fetch, cdnBase);
}

export { createCDNLoader as c };
