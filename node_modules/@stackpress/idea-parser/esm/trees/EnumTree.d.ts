import type { PropertyToken, DeclarationToken } from '../types.js';
import Lexer from '../Lexer.js';
import AbstractTree from './AbstractTree.js';
export default class EnumTree extends AbstractTree<DeclarationToken> {
    static definitions(lexer: Lexer): Lexer;
    static parse(code: string, start?: number): DeclarationToken;
    parse(code: string, start?: number): DeclarationToken;
    enum(): DeclarationToken;
    property(): PropertyToken;
}
