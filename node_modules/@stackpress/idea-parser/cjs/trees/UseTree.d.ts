import type { ImportToken } from '../types.js';
import Lexer from '../Lexer.js';
import AbstractTree from './AbstractTree.js';
export default class UseTree extends AbstractTree<ImportToken> {
    static definitions(lexer: Lexer): Lexer;
    static parse(code: string, start?: number): ImportToken;
    parse(code: string, start?: number): ImportToken;
    use(): ImportToken;
}
