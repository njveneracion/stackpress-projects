import type { TaskAction, ResponseStatus } from '../types.js';
import ItemQueue from './ItemQueue.js';
export default class TaskQueue<A extends Array<unknown>> extends ItemQueue<TaskAction<A>> {
    protected _after?: TaskAction<A>;
    protected _before?: TaskAction<A>;
    set after(action: TaskAction<A>);
    set before(action: TaskAction<A>);
    run(...args: A): Promise<ResponseStatus>;
}
