"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Status_js_1 = __importDefault(require("../Status.js"));
const ItemQueue_js_1 = __importDefault(require("./ItemQueue.js"));
class TaskQueue extends ItemQueue_js_1.default {
    set after(action) {
        this._after = action;
    }
    set before(action) {
        this._before = action;
    }
    run(...args) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.queue.length) {
                return Status_js_1.default.NOT_FOUND;
            }
            while (this.queue.length) {
                const task = this.consume();
                if (this._before && (yield this._before(...args)) === false) {
                    return Status_js_1.default.ABORT;
                }
                if (task && (yield task(...args)) === false) {
                    return Status_js_1.default.ABORT;
                }
                if (this._after && (yield this._after(...args)) === false) {
                    return Status_js_1.default.ABORT;
                }
            }
            return Status_js_1.default.OK;
        });
    }
}
exports.default = TaskQueue;
