import type { Item } from '../types.js';
export default class ItemQueue<I> {
    readonly queue: Item<I>[];
    protected _lower: number;
    protected _upper: number;
    get size(): number;
    add(item: I, priority?: number): this;
    push(item: I): this;
    shift(item: I): this;
    consume(): I | undefined;
}
