import type { Route, RouteMap, RouteAction } from '../types.js';
import type EventEmitter from './EventEmitter.js';
import ExpressEmitter from './ExpressEmitter.js';
export default class RouteEmitter<R = unknown, S = unknown> extends ExpressEmitter<RouteMap<R, S>> {
    readonly routes: Map<string, Route>;
    constructor();
    route(method: string, path: string, action: RouteAction<R, S>, priority?: number): this;
    use(emitter: EventEmitter<RouteMap<R, S>>): this;
    protected _eventNameFromRoute(method: string, path: string): string;
}
