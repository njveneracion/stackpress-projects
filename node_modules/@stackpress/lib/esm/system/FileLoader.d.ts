import type { FileSystem } from '../types.js';
export default class FileLoader {
    protected _cwd: string;
    protected _fs: FileSystem;
    get cwd(): string;
    get fs(): FileSystem;
    constructor(fs: FileSystem, cwd?: string);
    absolute(pathname: string, pwd?: string): Promise<string>;
    basepath(pathname: string): string;
    lib(pwd?: string): Promise<string>;
    modules(pathname: string, pwd?: string, meta?: boolean): Promise<string>;
    import<T = any>(pathname: string, getDefault?: boolean): Promise<T>;
    relative(pathname: string, require: string, withExtname?: boolean): string;
    resolve(pathname: string, pwd?: string, exists?: boolean): Promise<string | null>;
    resolveFile(pathname: string, extnames?: string[], pwd?: string, exists?: boolean): Promise<string | null>;
    protected _fileExists(pathname: string): Promise<boolean>;
}
