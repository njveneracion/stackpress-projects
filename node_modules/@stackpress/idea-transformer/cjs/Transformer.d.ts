import type { SchemaConfig, TypeConfig } from '@stackpress/idea-parser';
import type { FileLoaderOptions } from './types.js';
import FileLoader from '@stackpress/lib/FileLoader';
export default class Transformer<T extends Record<string, unknown>> {
    static load<T extends Record<string, unknown>>(input: string, options?: FileLoaderOptions): Promise<Transformer<T>>;
    readonly loader: FileLoader;
    readonly input: string;
    protected _schema?: SchemaConfig;
    constructor(input: string, loader: FileLoader);
    schema(): Promise<SchemaConfig>;
    transform(extras?: T): Promise<void>;
    protected _merge(parent: TypeConfig, child: TypeConfig): void;
}
