import type { TerminalOptions } from './types.js';
import Terminal, { control } from '@stackpress/lib/Terminal';
export { control };
export default class IdeaTerminal extends Terminal {
    static load(args: string[], options?: TerminalOptions): Promise<IdeaTerminal>;
    readonly cwd: string;
    readonly extname: string;
    constructor(args: string[], options?: TerminalOptions);
}
