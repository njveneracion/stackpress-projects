"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const map_1 = __importDefault(require("@stackpress/lib/map"));
const constants_js_1 = require("./constants.js");
class VirtualServer {
    constructor() {
        this.fs = (0, map_1.default)();
    }
    get(filepath) {
        const data = this.fs.get(filepath);
        if (typeof data === 'string') {
            return Buffer.from(data, 'base64').toString('utf8');
        }
        return null;
    }
    has(filepath) {
        return this.fs.has(filepath);
    }
    set(filepath, contents) {
        const data = Buffer.from(contents).toString('base64');
        this.fs.set(filepath, data);
        return `${constants_js_1.VFS_PROTOCOL}${filepath}`;
    }
}
exports.default = VirtualServer;
