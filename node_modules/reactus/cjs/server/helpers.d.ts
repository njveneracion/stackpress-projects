import type { ElementType } from 'react';
import type { UnknownNest } from '@stackpress/lib/types';
import NodeFS from '@stackpress/lib/NodeFS';
import type { ServerConfig } from './types.js';
export declare function configure(options: Partial<ServerConfig>): Readonly<{
    clientRoute: string;
    cssRoute: string;
    cwd: string;
    documentTemplate: string;
    fs: import("@stackpress/lib/types").FileSystem | NodeFS;
    pagePath: string;
}>;
export declare function id(content: string, length?: number): string;
export declare function renderJSX(element?: ElementType, props?: UnknownNest): string;
