import FileLoader from '@stackpress/lib/FileLoader';
import type { ServerConfig } from './types.js';
export default class Server {
    readonly loader: FileLoader;
    protected _paths: Record<string, string>;
    protected _routes: Record<string, string>;
    protected _templates: Record<string, string>;
    get cwd(): string;
    get fs(): import("@stackpress/lib/types").FileSystem;
    get paths(): Readonly<Record<string, string>>;
    get routes(): Readonly<Record<string, string>>;
    get templates(): Readonly<Record<string, string>>;
    constructor(config: ServerConfig);
    import<T = any>(pathname: string, extnames?: string[]): Promise<T>;
    resolve(pathname: string, extnames?: string[]): Promise<{
        filepath: string;
        basepath: string;
        extname: string;
    }>;
}
