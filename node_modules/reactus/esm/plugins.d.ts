import type { Plugin, ViteDevServer } from 'vite';
import type ServerLoader from './ServerLoader.js';
import type VirtualServer from './VirtualServer.js';
import type { IM, SR, Next } from './types.js';
import type Server from './Server.js';
export declare function css(cssFiles: string[]): Plugin;
export declare function file(loader: ServerLoader, extnames?: string[]): {
    name: string;
    resolveId(source: string, importer?: string): Promise<string | undefined>;
};
export declare function hmr(server: Server): (req: IM, res: SR, next: Next) => Promise<void>;
export declare function vfs(vfs: VirtualServer): {
    name: string;
    configureServer(server: ViteDevServer): void;
    resolveId(source: string, importer?: string): string | undefined;
    load(id: string): string | null | undefined;
};
