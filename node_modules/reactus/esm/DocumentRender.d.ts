import type { UnknownNest } from '@stackpress/lib/types';
import type Document from './Document.js';
import type Server from './Server.js';
export default class DocumentRender {
    protected _document: Document;
    protected _server: Server;
    constructor(document: Document);
    renderHMRClient(): Promise<string>;
    renderMarkup(props?: UnknownNest): Promise<string>;
    protected _renderDevMarkup(props?: UnknownNest): Promise<string>;
    protected _renderMarkup(props?: UnknownNest): Promise<string>;
    protected _renderVFS(name: string, template: string): Promise<string>;
}
