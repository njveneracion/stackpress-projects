import type { UnknownNest } from '@stackpress/lib/types';
import type { ServerConfig, DocumentImport } from './types.js';
import Document from './Document.js';
import Server from './Server.js';
import { BASE62_ALPHABET, HASH_LENGTH, DOCUMENT_TEMPLATE } from './constants.js';
import { configure, id, renderJSX } from './helpers.js';
export type { UnknownNest, ServerConfig, DocumentImport };
export { BASE62_ALPHABET, HASH_LENGTH, DOCUMENT_TEMPLATE, configure, id, renderJSX, Document, Server };
export declare function serve(options: Partial<ServerConfig>): {
    config: Readonly<{
        clientRoute: string;
        cssRoute: string;
        cwd: string;
        documentTemplate: string;
        fs: import("@stackpress/lib/types").FileSystem | import("@stackpress/lib/NodeFS").default;
        pagePath: string;
    }>;
    paths: Readonly<Record<string, string>>;
    routes: Readonly<Record<string, string>>;
    templates: Readonly<Record<string, string>>;
    server: Server;
    render: (entry: string, props?: UnknownNest) => Promise<string>;
};
