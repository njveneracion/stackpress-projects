'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { useState } from 'react';
import R22nContext from '../context.js';
const R22nProvider = (props) => {
    const { children, ...config } = props;
    if (!config.translations) {
        config.translations = {};
    }
    const [language, set] = useState({
        language: config.language || 'en_US',
        translations: config.translations || {}
    });
    const changeLanguage = (language, translations) => {
        if (!translations) {
            translations = {};
        }
        set({ language, translations });
    };
    const value = {
        placeholder: config.placeholder || '%s',
        language: language.language,
        translations: language.translations,
        changeLanguage
    };
    return (_jsx(R22nContext.Provider, { value: value, children: children }));
};
export default R22nProvider;
