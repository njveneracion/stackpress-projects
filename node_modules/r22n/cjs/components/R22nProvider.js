"use strict";
'use client';
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const jsx_runtime_1 = require("react/jsx-runtime");
const react_1 = require("react");
const context_js_1 = __importDefault(require("../context.js"));
const R22nProvider = (props) => {
    const { children } = props, config = __rest(props, ["children"]);
    if (!config.translations) {
        config.translations = {};
    }
    const [language, set] = (0, react_1.useState)({
        language: config.language || 'en_US',
        translations: config.translations || {}
    });
    const changeLanguage = (language, translations) => {
        if (!translations) {
            translations = {};
        }
        set({ language, translations });
    };
    const value = {
        placeholder: config.placeholder || '%s',
        language: language.language,
        translations: language.translations,
        changeLanguage
    };
    return ((0, jsx_runtime_1.jsx)(context_js_1.default.Provider, { value: value, children: children }));
};
exports.default = R22nProvider;
