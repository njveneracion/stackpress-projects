"use strict";
'use client';
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = useLanguage;
const jsx_runtime_1 = require("react/jsx-runtime");
const react_1 = require("react");
const context_js_1 = __importDefault(require("../context.js"));
function useLanguage() {
    const { language, placeholder, translations, changeLanguage } = (0, react_1.useContext)(context_js_1.default);
    const engine = {
        language,
        changeLanguage,
        template(phrase) {
            var _a;
            const translation = (_a = translations[phrase]) !== null && _a !== void 0 ? _a : phrase;
            const chunks = translation.split(placeholder);
            return function Translation(...variables) {
                if (translation.indexOf(placeholder) < 0)
                    return translation;
                const children = [];
                chunks.forEach((chunk, i) => {
                    children.push(chunk);
                    if (variables[i]) {
                        children.push(variables[i]);
                    }
                });
                const allStrings = children.filter(child => typeof child === 'string').length === children.length;
                if (allStrings) {
                    return children.join('');
                }
                return ((0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [...children] }));
            };
        },
        t(phrase, ...variables) {
            return engine.template(phrase.join(placeholder))(...variables);
        },
        _(phrase, ...variables) {
            return engine.template(phrase)(...variables);
        }
    };
    return engine;
}
;
